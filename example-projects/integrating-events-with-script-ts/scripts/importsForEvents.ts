
// This is a function to add two numbers called from
// the event sheet. See the event sheet for how it's used.
// Note this method is defined in the imports for events
// script, so scripts in events can directly call it.
function add(a: number, b: number)
{
	return a + b;
}